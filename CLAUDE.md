# ani-website

Personal website for Ani Kochiashvili.

## gstack

gstack v0.12.9.0 is installed at `.claude/skills/gstack/`.

Available skills:
- `/gstack` — headless browser QA testing and site dogfooding
- `/ship` — full ship workflow (plan → review → test → deploy)
- `/qa` — QA testing with fix loop
- `/qa-only` — report-only QA, no fixes
- `/review` — PR review
- `/autoplan` — auto-review pipeline (CEO → design → eng)
- `/plan-ceo-review` — CEO-style product review
- `/plan-eng-review` — engineering review
- `/plan-design-review` — design audit
- `/design-review` — design audit with fix loop
- `/design-consultation` — design system from scratch
- `/investigate` — systematic root-cause debugging
- `/browse` — headless browser navigation
- `/retro` — retrospective
- `/office-hours` — YC Office Hours startup diagnostic
- `/benchmark` — performance regression detection
- `/canary` — post-deploy monitoring loop
- `/land-and-deploy` — merge → deploy → verify
- `/setup-deploy` — one-time deploy config
- `/document-release` — post-ship doc updates
- `/cso` — OWASP Top 10 + STRIDE security audit
- `/freeze` / `/unfreeze` — freeze/unfreeze shipping
- `/careful` — careful mode
- `/guard` — guard mode
- `/codex` — multi-AI second opinion via OpenAI Codex CLI
